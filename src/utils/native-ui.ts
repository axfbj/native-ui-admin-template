import {
  // create naive ui
  create,
  // component
  NButton,
  NSpace,
  NSwitch,
  NInputNumber
} from 'naive-ui'

const naive = create({
  components: [NSpace, NButton, NSwitch, NInputNumber]
})

export default naive
